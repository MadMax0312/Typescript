const heros: string[] = []
const studentId: number[] = []

const studentName: Array<string> = []
const classNumber: Array<number> = []

type User = {
    name: string;
    isActive: boolean;
}

const allUsers: User[] = []

const MLModels: number[][] = [
    [255, 255, 255],
    []
]

studentId.push(2)
heros.push('batman')
allUsers.push({name:'jack', isActive:true})